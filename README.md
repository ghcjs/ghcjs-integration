This repository contains submodules and project files for working on various GHCJS libraries before uploading them to hackage.org.

To start a `nix develop` shell use:

```
nix develop '.?submodules=1#'
```
